# Assets

Store future issue assets here, including brand files, source images, generated previews, and reusable graphics.

The live iMessage/Open Graph preview currently uses `../og-image.jpg` at the repository root.

Current AVC brand asset:

- `avc-logo-full-dark.svg` for dark header/footer placement.
